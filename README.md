# Sistema de Pagamentos Multi-Provider

### Instruções para rodar o projeto

Use o comando <kbd>docker-compose up -d</kbd> e após o docker subir seu container, voce precisa rodar os comandos <kbd>docker-compose exec app php artisan migrate</kbd>

### Outras informações
A aplicação está usando a porta 8080
